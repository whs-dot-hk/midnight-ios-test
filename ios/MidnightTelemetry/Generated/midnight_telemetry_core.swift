// Placeholder — overwritten by `rust/scripts/build-ios.sh`, which runs
// uniffi-bindgen against the compiled Rust core to produce the real bindings
// (TelemetryClient, Snapshot, NodeInfo, AlertEvent, TelemetryDelegate, ...).
// The app will not compile until that script has been run at least once.
